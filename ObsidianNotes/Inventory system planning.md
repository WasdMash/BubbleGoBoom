Should implement a public function which:
- Takes an index in the chest and adds it to the player inventory at another index
	- I can drag stuff between the inventory just fine and copy (not move) stuff from the chest to the inventory
		- However, there's no dropping stuff off into the chest or moving stuff around within the chest. Why? That's your problem to figure out.

- **Data Storage:** The manager saves a complete list of all `ChestData` objects to a single save file (e.g., `chests.sav`) . 

